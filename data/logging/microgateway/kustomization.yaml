apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: kibana-microgateway-with-iam-config
    files:
      - config-with-iam/config.yaml
  - name: kibana-microgateway-without-iam-config
    files:
      - config-without-iam/config.yaml
